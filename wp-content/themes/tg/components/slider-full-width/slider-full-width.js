document.addEventListener('DOMContentLoaded', () => {
  //*********************************/
  // slider-full-width
  // @version 1.0
  // @package tg
  //*********************************/

  const swiper = new Swiper('#slider-full-width', {
    pagination: {
      el: '.swiper-pagination',
      dynamicBullets: true,
    },
  });
});